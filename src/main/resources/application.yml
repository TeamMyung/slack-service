spring:
  datasource:
    url: jdbc:postgresql://localhost:5438/slack_db
    username: postgres
    password: postgres
    driver-class-name: org.postgresql.Driver
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
server:
  port: 8086

slack:
  bot:
    token: ${SLACK_BOT_TOKEN}